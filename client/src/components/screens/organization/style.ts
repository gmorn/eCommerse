import { styled } from 'styled-components'

export const OrgPageContainer = styled.div`
	margin-top: 20px;
`

export const OrgHeader = styled.div`
	display: flex;
	align-items: center;
	justify-content: space-between;
`